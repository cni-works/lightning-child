# プロジェクト方針

## 目的

年額ライセンスを希望しないクライアント向けに、無料テーマLightningを土台として、実務上なくて困る機能だけを補う再利用可能な子テーマを作ります。

## 対象

- 無料テーマLightningの現行G3世代
- WordPressの標準機能
- 無料で利用できるLightning推奨プラグイン
- 子テーマで安全に追加できるCSS、PHP、JavaScript、テンプレート

## 対象外

- Lightning G3 Pro Unitを前提とした実装
- VK Blocks Proを前提とした実装
- 有料製品の完全な再現
- 有料製品のコードや非公開素材のコピー
- 親テーマLightning本体の直接変更
- 本番サイトへの直接反映

## 機能を追加する判断基準

1. 無料版にないことで、実際の案件作業や利用者の運用が困るか。
2. 複数案件で再利用できるか。
3. WordPress標準APIまたはLightningの公開フックで安全に実装できるか。
4. 親テーマ更新後も保守できる規模か。
5. 子テーマに置くべき機能か、独自プラグインに置くべき機能か。
6. 得られる利便性に対して、将来の動作確認や修正負担が大きすぎないか。

## 保守性を優先する方針

機能を増やすことよりも、LightningやWordPressの更新後に壊れにくいことを優先します。

### 優先する実装

- CSS変数と小規模な追加CSS
- WordPress標準API
- Lightningが公開しているアクションフックとフィルターフック
- 一つの目的に限定した短いPHP処理
- 管理画面を増やさず、既存設定を利用する実装

### 慎重に判断する実装

- 親テーマのテンプレート上書き
- Lightning内部のHTML構造やクラス名へ強く依存するCSS
- 独自JavaScriptによる既存UIの置き換え
- 独自設定画面やデータ保存
- 複数ファイルにまたがる大きな機能

### 原則として避ける実装

- 親テーマのテンプレートを大量にコピーすること
- Lightningの非公開または内部実装へ依存すること
- 有料製品の機能を一式再現すること
- 更新のたびに継続的な追従が必要になる複雑な機能
- 実案件で利用頻度が低い機能

## 実装場所の基準

### 子テーマ

- 配色、余白、文字、見出しなどのデザイン調整
- ヘッダー、フッター、投稿レイアウトなどLightning固有の表示調整
- Lightningのテンプレートやフックに依存する処理
- サイト表示に必要なCSSとJavaScript

### 独自プラグイン

- テーマを変更しても保持したい機能
- 管理画面の設定や保存データ
- ショートコード、独自ブロック、独自投稿タイプ
- 複数テーマで利用したい共通機能

## 最初に決めること

- 無料版で「なくて困る」機能の候補
- その中で利用頻度が高い上位3件
- 対象とするWordPress・PHP・Lightningのバージョン
- 一つの共通子テーマとして配布するか、案件ごとに複製して調整するか

## 現時点で決定した初期要件

ブロック追加機能全般は後回しにし、まずは無料版Lightningの表示調整に必要な機能を優先します。

### 固定ページごとの非表示設定

以下を固定ページの編集画面から個別に切り替えられるようにします。

- ヘッダーを表示しない
- ページヘッダーを表示しない
- パンくずリストを表示しない
- フッターを表示しない

ハンバーガーボタンとモバイル固定ナビのページ別非表示設定は実装しません。

### ヘッダー

ヘッダーは重要機能として扱います。ただし、初期版ではブロックテンプレートパーツ化せず、無料版Lightning標準のヘッダーを利用して安全に拡張します。

候補となる設定は以下です。実装前に親テーマの公開フック、HTML構造、CSSを調査し、保守可能な範囲に絞ります。

- Lightning標準と調整版の限定的なレイアウト切り替え
- ヘッダー背景色と文字色
- トップページの透過ヘッダー
- 透過時の文字色
- 透過時またはスクロール時のロゴ画像
- スクロール後の背景色
- モバイルでのロゴ位置

複数の複雑なレイアウト、細かな透過度やグラデーション設定、ヘッダー高さ検出の最適化設定は初期対象外とします。

### フッター

- Lightning標準フッターの背景色と文字色を変更可能にする
- フッター背景画像とウィジェットカラム数の変更は初期対象外とする
- Lightning標準フッターと、ブロックで構築するカスタムフッターを選択できる構成を検討する
- カスタムフッターはWordPress標準ブロックと無料で利用可能なブロックだけで構築し、VK Blocks Proの「固定ページ本文」ブロックに依存しない
- ブロック式フッターの保存場所を子テーマと補助プラグインのどちらにするかは、実装前の設計で判断する

### コピーライト

- 無料版Lightning標準のテーマクレジットを、独自のコピーライト表記へ変更できるようにする
- コピーライトを空欄にした場合は非表示にする
- `{year}`を現在年、`{site_name}`をサイト名へ置換できる形式を候補とする
- ブロック式フッター使用時は標準コピーライトを重複表示しない

### 投稿・カスタム投稿の詳細ページ

投稿タイプごと、または安全に実現できる共通設定として、以下の非表示設定を検討します。

- 公開日
- 更新日
- 投稿者名
- 前の記事・次の記事

### フォント

- ヘッダーロゴ、グローバルメニュー、タイトル・見出し、本文テキストを場所ごとに指定できるようにする
- 端末標準のゴシック体・明朝体に加え、Google Fonts の Noto Sans JP と Noto Serif JP を選択可能にする
- Google Fonts は選択された書体だけを通常・太字（400 / 700）で読み込み、未選択時は外部フォントを読み込まない
- 既存のサイトフォント設定は本文設定として引き継ぎ、追加項目は本文フォントを継承する

### 右追尾ボタン

- お問い合わせとLINE用の固定ボタンを最大2つ設定できるようにする
- 各ボタンでテキスト、Font Awesomeクラス、URL、別ウィンドウ、背景色、文字色を設定する
- 表示タイミング、左右位置、上からの位置、画面端の余白、サイズ、角丸を共通設定とする
- 有効時は全画面サイズで表示し、必要な案件ではスマートフォンだけ非表示にできるようにする
- 親テーマのスクロール用クラスやVK BlocksのHTMLへ依存せず、子テーマ独自の出力と小規模なJavaScriptで制御する

### 追加で実装した機能

- ブロック式カスタムヘッダー
- Lightning標準ヘッダーのPCファーストビュー／常時ロゴ非表示とモバイルロゴ非表示
- モバイル固定ナビ（フッターテンプレートパーツまたはマイパターンを専用ドロワーへ表示し、通常フッター表示を維持）
- 固定ページ・投稿・公開カスタム投稿タイプごとのキャッチフレーズ、ページヘッダー画像、サブテキスト、オーバーレイ
- 投稿タイプごとの個別ページ／アーカイブのページヘッダー非表示

### 後回しにする機能

- VK Blocks Pro相当のブロック追加
- SEOプラグインなど他の一般的なプラグインで代用できる機能
- 有料版の設定項目を網羅的に再現すること

## 最初の作業手順

1. 親テーマ `lightning` は変更せず、現行G3の出力箇所と公開フックを調査する。
2. 子テーマの現状を確認し、不要な読み込みや親テーマとの重複がないか調べる。
3. 各候補を「公開フックで可能」「親HTMLへの依存あり」「テンプレート上書きが必要」に分類する。
4. 保守負担の低い機能から、一つずつ実装・確認する。
