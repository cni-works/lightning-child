# Lightning Child 作業ルール

- このフォルダだけを独立したLightning子テーマとして扱います。
- 親テーマLightningのファイルは直接変更しません。
- 無料テーマLightningの現行G3世代を対象とし、テンプレートを調べる場合は親テーマの `_g3` 配下を優先します。
- 有料プラグイン「Lightning G3 Pro Unit」と「VK Blocks Pro」の機能がある前提で実装しません。
- 有料製品のコードをコピーせず、WordPress標準APIと公開されているLightningのフックを使って独自実装します。
- 無料版で不足する機能をすべて再現せず、利用者が実務上必要とする機能だけを小さく追加します。
- 機能数より保守性を優先し、CSS変数、WordPress標準API、Lightningの公開フックで実現できる方法を優先します。
- 親テンプレートのコピー、内部HTML構造への強い依存、複雑な独自JavaScriptは、他に安全な方法がない場合だけ提案します。
- 将来の確認・修正負担が利便性を上回る機能は実装せず、より小さな代替案を提示します。
- テーマ固有ではない機能、保存データ、管理画面機能は、子テーマへ無理に入れず独自プラグイン化を提案します。
- 親テーマのファイルを子テーマへ丸ごとコピーせず、必要なフック・テンプレート・処理だけを追加します。
- 依頼されていないサイトデザインや機能は変更しません。
- 大きな一括変更を避け、CSS、PHP、JavaScript、テンプレートを目的ごとに小さく変更します。
- PHPではWordPress標準API、適切なエスケープ、サニタイズ、権限・nonce確認を使用します。
- 変更後は、変更ファイル、確認結果、WordPress上の手動確認手順を説明します。
- GitHubへの接続、push、公開、本番サイトへの反映は行いません。
